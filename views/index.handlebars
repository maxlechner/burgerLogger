<h1>Eat the burger</h1>
<img src="http://theworldsbestever.s3.amazonaws.com/blog/wp-content/uploads/2012/02/bbbbuger.gif" alt="burger GIF" width="25%">

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <p>
        {{this.id}}.{{this.burger_name}}
        <button class="devour-burger" data-id=“{{this.id}}” id=“devour”>Devour it!</button>
        
      </p>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
        <p>
          {{this.id}}.{{this.burger_name}}
          <button class="delete-burger" data-id=“{{this.id}}” id=“delete”>Delete it!</button>
        </p>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger </h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Eaten!<br>
    <input type="radio" name="devoured" value="0"> Not eaten
  </div>
  <button type="submit">Add Burger</button>
</form>
